# CoWorks Server

Minimal Express + Mongoose backend for CoWorks.

Environment variables:
- `MONGODB_URI` — mongo connection string
- `JWT_SECRET` — secret for JWT tokens

Scripts:
- `npm run dev` — start with nodemon
- `npm run seed` — run seed script
